version: '3.9'

services:

  postgres:
    image: pgvector/pgvector:pg16
    container_name: chatbot-postgres
    restart: always

    environment:
      POSTGRES_DB: chatbotdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Brujo2026

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: